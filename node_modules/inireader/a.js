var events = require('events').EventEmitter;
var inireader = require('lib/inireader').IniReader;
var utils = require('util');

function Z() {
}

utils.inherits(Z, inireader)
console.log(utils.inspect(Z, true));

